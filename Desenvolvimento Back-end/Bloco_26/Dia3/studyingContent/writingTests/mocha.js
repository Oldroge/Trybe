// Structuring tests with Mocha;

describe('When the average were less than 7', () => {
  //
})

//Describe accept 2 params: the first one is the description that put a resum about what the test tests, and second and the second execute a function to execute the test scenary. It's not necessary import the mocha, your reserved words will be interpreted when execute the test.

// Now using the it syntax is similar the describe, and always is used inside a describe, the it describe what really will be tested:
describe('When the average were less than 7', () => {
  it('retorna "reprovado"', () => {
    //
  });
})
