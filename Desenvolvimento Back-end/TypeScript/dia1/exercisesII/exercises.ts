// Exercise 1: Create a new function to calculate an diamond area. The area is given by multiply results from bigger diagonal (D) through the less (d) divide by two. (D * d) / 2

export function calculateDiamond(D: number, d: number) {
  return (D * d) / 2
}
