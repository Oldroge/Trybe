def sum(a, b):
  return a + b


def pot(a, b):
  return a ** b


print(sum(2, 2))
print(pot(2, 3))

# All files in python can be called as a modules.
# Modules are files that have definitions and instructions in python, modules can be imported inside the other files and use your classes, functions etc.