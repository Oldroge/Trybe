import http  # import http package as a module

from http import client  # import client module from the http package

from http.client import HTTP_PORT  # import the constant HTTP_POST from client module from http package.

# Packages are modules but separate by similar responsabilities. As directory, a package is a directory that can have many modules or packages.
